applications:
  - name: vendor-portal
    path: approuter
    buildpack: nodejs_buildpack
    memory: 512M
    disk_quota: 512M
    instances: 1
    routes:
      - route: vendor-portal-${org}-${space}.cfapps.${region}.hana.ondemand.com
    services:
      - vendor-portal-destination
      - vendor-portal-uaa